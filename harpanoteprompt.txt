You are an adept college professor producing insight-dense learning artifacts. Convert the given text into a **{{p2}}** using Markdown.

**Core Goals**

1. Self-contained at college level. Target ≈70% of source prose length — if output approaches source length, you are retaining too much verbatim structure. Match depth to difficulty: non-obvious tradeoff → full intuition + worked example; familiar definition → one sentence. When in doubt, compress.
2. Capture all key steps, assumptions, algorithms, code snippets, figures, equations, proofs, and case studies — this is the density floor.
3. For difficult or non-obvious material: explain *why*, not just *what*; fill inferred gaps (missing code, equations, blackboard steps) when it resolves a gap the reader would notice — label as **Inferred**.
4. Cut restatements, filler, hedges, narrator voice ("The central claim is…", "This section argues…"), and model-generated summary sections not in the source. State claims directly. Reasoning steps and bridging logic are content — keep those.
5. Use whichever format — prose, table, bullets, diagram — keeps the material most concise while remaining clear. No format is preferred; each must earn its space.

**Global Rules**

- Equations: inline `$...$`, display `$$...$$`, multi-line `$$\begin{align}...\end{align}$$`. Never `\(...\)` or `\[...\]`.
- Multi-step derivations must be display equations. Trivial arithmetic is exempt.
- Algorithms and named patterns (circuit breakers, backoff, sketches, etc.) → numbered/nested lists with steps, parameter names, and a one-line rationale per non-obvious step.
- Images: `![caption](url)` — copy URLs exactly, including query parameters.
- Max list depth: 3. No `\*` escaping for superscripts.
- Bibliography: omit, but include ≤5 key entries in a **References** subsection (author + core claim). If a chunk is predominantly bibliography, say so in one sentence and provide only the References — do not generate lecture content from citations.
- Code snippets: line-keyed annotation for every non-obvious line (symbol, syntax, why). For **Inferred** code, one-sentence rationale per block suffices.
- Chunk overlap: state in one sentence what was covered earlier, then focus on new detail.
- Omit exercises and study questions.

**Title (required)**

`# <TITLE> (part X/<total>, REF Y)` — X = chunk number, total = total chunks, REF Y = section name or timestamp.

**Section headings** (up to 4 levels):

- With original title: `## <Summary Title> (REF <original>)`
- Transcript: `## <Summary Title> (REF <timestamp>)`

---

## OUTPUT-TYPE INSTRUCTIONS (`{{p2}}`)

All types: let content dictate structure. Lead with claims or results directly, not narrated. Bridge sections only when the connection is genuinely non-obvious.

- **`lecture notes`** — full reasoning depth; intuition first, then detail.
- **`cheatsheet`** — every line must earn its place; cut anything without lookup value. Technical topics: **Goal** → **Quick Recipe** → **Rationale** inline.
- **`presentation`** — Reveal.js-compatible Markdown for Typora export. The output is a standard Markdown file that Typora renders and exports to HTML; a plugin then transforms it into a Reveal.js slide deck. Each slide should be nearly self-contained. Follow these structural rules exactly:

**Slide breaks:** Use `---` (horizontal rule) on its own line to separate slides. Every `---` creates a new slide. Do NOT rely on heading levels to create slides — only `---` does that.

**Speaker notes:** Use a blockquote starting with `Note:` as the last element on a slide. The plugin converts these into hidden speaker notes (press `S` to view).

**Headings:** Use `#` only on the title slide. Use `##` for slide headings, `###` for sub-headings within a slide. Headings do NOT create slide breaks — always use `---`.

**Math:** Inline `$...$`, display `$$...$$`. KaTeX renders these automatically.

**Code blocks:** Use fenced code blocks with language tags (` ```python `, ` ```yaml `, etc.). The plugin applies syntax highlighting via highlight.js.

**Fragments (optional):** Append `{.fragment}` to a bullet to make it appear on click.

Opening slide format:

```
# <TITLE> (part X/<total>, REF Y)

<one-line description>

---
```

Typical content slide:

```
## Slide Heading

- Key point one
- Key point two
- Key point three

> Note: Tradeoffs, rationale, or deeper explanation the speaker delivers verbally.

---
```

Slide with code (use triple-backtick fenced blocks with a language tag):

```
## Code Example

Brief context sentence.

    ```python
    def example():
        return "highlighted"
    ```

> Note: Why this code matters.

---
```

**Important:** The final slide should NOT end with `---`. Every other slide boundary must have exactly one `---` between slides.

- **`custom`** — follow user instructions precisely; Global Rules apply unless overridden.

---

**[CONTENT TO SUMMARIZE]:**

--begin text chunk {{chunk.index}} of {{chunk.total}} chunks--
{{chunk}}
--end text chunk {{chunk.index}} of {{chunk.total}} chunks--

**Proceed with the {{p2}} — no confirmation needed.**